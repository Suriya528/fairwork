import { apiFetch } from "./apiClient"

interface BackendDispute { _id: string; projectId: string; raisedBy: string | { _id: string; firstName: string; lastName: string }; reason: string; evidence: string[]; status: "pending" | "resolved"; winner: "client" | "freelancer" | "none"; clientVotes: number; freelancerVotes: number; blockchainTxn: string; createdAt: string }
export interface ApiDispute { id: string; projectId: string; raisedById: string; raisedByName: string | null; reason: string; evidence: string[]; status: "pending" | "resolved"; winner: "client" | "freelancer" | "none"; clientVotes: number; freelancerVotes: number; blockchainTxn: string; createdAt: string }
function toApiDispute(d: BackendDispute): ApiDispute { const p = d.raisedBy; return { id: d._id, projectId: d.projectId, raisedById: typeof p === "string" ? p : p._id, raisedByName: typeof p === "string" ? null : `${p.firstName} ${p.lastName}`.trim(), reason: d.reason, evidence: d.evidence ?? [], status: d.status, winner: d.winner, clientVotes: d.clientVotes, freelancerVotes: d.freelancerVotes, blockchainTxn: d.blockchainTxn, createdAt: d.createdAt } }
export async function raiseDispute(projectId: string, reason: string, token: string): Promise<ApiDispute> { return toApiDispute(await apiFetch<BackendDispute>("/disputes", { method: "POST", token, body: { projectId, reason } })) }
export async function getDispute(projectId: string, token: string): Promise<ApiDispute | null> { const data = await apiFetch<BackendDispute | null>(`/disputes/${projectId}`, { token }); return data ? toApiDispute(data) : null }
