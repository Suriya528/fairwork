import { FiCheckCircle, FiDollarSign, FiFolder, FiTrendingUp } from "react-icons/fi"; import { MetricCard } from "@/components/common/MetricCard"; import type { ApiAnalytics } from "@/services/analyticsApi"; import { formatCurrency } from "@/lib/format"
export function StatsGrid({ data, role }: { data: ApiAnalytics | null; role: "client" | "freelancer" }) { if (!data) return null; return <section className="grid gap-3 sm:grid-cols-2 lg:grid-cols-4"><MetricCard label="Total projects" value={String(data.totalProjects)} icon={FiFolder}/><MetricCard label="Completed" value={String(data.completedProjects)} icon={FiCheckCircle}/><MetricCard label={role === "freelancer" ? "Earnings" : "Spent"} value={formatCurrency(data.totalEarnings ?? data.totalSpent ?? 0)} icon={FiDollarSign}/><MetricCard label="Success rate" value={`${data.successRate}%`} icon={FiTrendingUp}/></section> }
