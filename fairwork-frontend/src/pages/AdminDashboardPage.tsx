import { FiTool } from "react-icons/fi"; import { PagePlaceholder } from "@/components/common/PagePlaceholder"
export function AdminDashboardPage() { return <PagePlaceholder title="Admin dashboard" description="Admin access is intentionally unavailable until the backend has an admin role and authorization schema." icon={FiTool}/> }
